import { Canvas, Meta, Story } from "@storybook/addon-docs";

import { ProjectCard } from "../../components/Projects.tsx"

<Meta title="Project Card" component={ProjectCard} />

export const Template = (args) => <ProjectCard {...args} />;

# ProjectCard

The ProjectCard component is meant to showoff each of my
projects showing:
- A short animation
- The title
- A short description
- A list of tools in the form of icon svgs